<h1>Listing operation_types</h1>

<%=@req %>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @operation_types.each do |operation_type| %>
      <tr>
        <td><%= operation_type.title %></td>
        <td><%= link_to 'Show', operation_type %></td>
        <td><%= link_to 'Edit', edit_operation_type_path(operation_type) %></td>
        <td><%= link_to 'Destroy', operation_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Operation type', new_operation_type_path %>
